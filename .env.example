# StreamVault v2 — Environment
# Run 'streamvault setup' to configure interactively

# Keep 7005 when using docker-compose (the port mapping expects it)
PORT=7005
HOST=0.0.0.0
NODE_ENV=production

# TorBox — torbox.app/settings
TORBOX_API_KEY=
TORBOX_API_URL=https://api.torbox.app/v1/api
TORBOX_SEARCH_API_URL=https://search-api.torbox.app
TORBOX_TIMEOUT_MS=10000
TORBOX_RETRIES=2
TORBOX_ENABLE_NATIVE_SEARCH=true
TORBOX_ENABLE_USENET=true
TORBOX_SEARCH_USER_ENGINES=false
TORBOX_PROVIDER_PRIORITY=torbox-torrent,torbox-usenet,library,torrentio,knightcrawler
EXTERNAL_TORRENT_FALLBACK=true
TORRENTIO_ENABLED=true
KNIGHTCRAWLER_ENABLED=true

# Dashboard/CLI admin password
ADMIN_PASSWORD=

# Signing key for stream URLs — generate with: openssl rand -hex 32
SECRET_KEY=

# Public URL — your own domain/reverse-proxy URL, for example https://your-hostname.example
PUBLIC_BASE_URL=

# Local IP of this machine
NAS_LOCAL_IP=

# Redis (optional but recommended)
REDIS_URL=redis://127.0.0.1:6379

# Cache TTL in seconds
STREAM_CACHE_TTL=1800
META_CACHE_TTL=7200

# Profile defaults
DEFAULT_MIN_QUALITY=1080p
DEFAULT_CACHED_ONLY=true
DEFAULT_LANGUAGE=en

# Cloudflare (optional — set by setup wizard)
CF_TUNNEL_TOKEN=
CF_DOMAIN=
